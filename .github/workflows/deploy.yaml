name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    strategy:
      matrix:
        include:
          - env: testnet
            rpc_url: testnet
            chain_id: "0x534e5f474f45524c49"
          - env: testnet2
            rpc_url: testnet2
            chain_id: "0x534e5f474f45524c4932"
          - env: mainnet
            rpc_url: mainnet
            chain_id: "0x534e5f4d41494e"

    runs-on: ubuntu-latest
    name: Deploy to Cloudflare
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install

      - name: Publish
        uses: cloudflare/wrangler-action@2.0.0
        with:
          apiToken: ${{ secrets.CF_API_TOKEN }}
          command: |
            deploy --name nft_metadata --env ${{ matrix.env }} --var STARKNET_RPC_URL:${{ matrix.rpc_url }} --var STARKNET_CHAIN_ID:${{ matrix.chain_id }}