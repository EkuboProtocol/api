name: Deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:


concurrency: deploy

jobs:
  deploy:
    strategy:
      matrix:
        include:
          - network_name: mainnet
          - network_name: goerli

    runs-on: ubuntu-latest
    name: Deploy to Cloudflare
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: npm

      - name: Install dependencies
        run: npm install

      - name: Publish
        uses: cloudflare/wrangler-action@2.0.0
        with:
          apiToken: ${{ secrets.CF_API_TOKEN }}
          command: |
            deploy --env ${{ matrix.network_name }} --var PGPASSWORD:${{ secrets.PGPASSWORD }}